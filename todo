				// update(&p1, coord);
				// update(&p2, coord);
				// update(&p3, coord);
				// p1 = iso(p1);
				// p2 = iso(p2);
				// p3 = iso(p3);
				// center(&p1);
				// center(&p2);
				// center(&p3);
to fix in fdf.c
